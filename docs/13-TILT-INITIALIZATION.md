# Автоматическая инициализация при запуске Tilt

> Полностью автоматический startup без ручных команд

---

## 🚀 Что происходит при `make tilt-up`

```
┌─────────────────────────────────────────────────────────┐
│                    make tilt-up                         │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  1. Check Kubernetes                                    │
│     └─ kubectl version                                  │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  2. Create Namespace                                    │
│     └─ kubectl apply -f k8s/namespace.yaml              │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  3. Start Infrastructure                                │
│     ├─ Redpanda (Kafka)                                 │
│     ├─ Schema Registry                                  │
│     ├─ Redis                                            │
│     ├─ PostgreSQL (Citus Coordinator)                   │
│     ├─ Citus Workers                                    │
│     ├─ Loki                                             │
│     ├─ Promtail                                         │
│     └─ Grafana                                          │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│  4. Wait for Ready                                      │
│     └─ Health checks pass                               │
└────────────────┬────────────────────────────────────────┘
                 │
                 ├──────────────────────────┐
                 │                          │
                 ▼                          ▼
    ┌────────────────────┐    ┌────────────────────────┐
    │  5a. Apply         │    │  5b. Register          │
    │      Migrations    │    │      Schemas           │
    │                    │    │                        │
    │  Depends:          │    │  Depends:              │
    │  - citus-coord     │    │  - schema-registry     │
    │                    │    │                        │
    │  Runs:             │    │  Runs:                 │
    │  V1__create...sql  │    │  BetEvent.avsc        │
    │  V2__seed...sql    │    │  PaymentEvent.avsc    │
    │  V3__idempot...sql │    │  BalanceEvent.avsc    │
    │  V4__views.sql     │    │  ComplianceEvent.avsc │
    │  V5__triggers.sql  │    │  SystemEvent.avsc     │
    │                    │    │  TenantEvent.avsc     │
    └────────┬───────────┘    └────────┬───────────────┘
             │                         │
             └──────────┬──────────────┘
                        │
                        ▼
        ┌───────────────────────────────┐
        │  6. Start Microservices       │
        │                               │
        │  Auto-discovered from:        │
        │  packages/**/Dockerfile       │
        │                               │
        │  With:                        │
        │  - Live reload (HMR)          │
        │  - Port forwarding            │
        │  - Log streaming              │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │  ✅ Ready for Development!    │
        │                               │
        │  Access via:                  │
        │  - http://localhost:10350     │
        │    (Tilt UI)                  │
        │                               │
        │  - PostgreSQL: 5432           │
        │  - Redis: 6379                │
        │  - Kafka: 19092               │
        │  - Schema Registry: 8081      │
        │  - Grafana: 3000              │
        └───────────────────────────────┘
```

---

## ⏱️ Время запуска

| Этап | Время | Параллельно |
|------|-------|-------------|
| Check K8s | <1s | - |
| Create namespace | <1s | - |
| Start infra | 10-30s | ✅ |
| Apply migrations | 2-5s | ✅ (с регистрацией схем) |
| Register schemas | 2-3s | ✅ (с миграциями) |
| Start services | 5-15s | ✅ |
| **Total** | **~20-60s** | - |

---

## 🔧 Ручной режим (если нужен контроль)

### Шаг 1: Только инфраструктура
```bash
make tilt-up
# Ctrl+C после готовности инфраструктуры
```

### Шаг 2: Миграции вручную
```bash
make migrate
```

### Шаг 3: Схемы вручную
```bash
make register-schemas
```

### Шаг 4: Продолжить Tilt
```bash
make tilt-up
```

---

## 📝 Триггеры автоматического обновления

### Миграции применяются при:
- ✅ Первом запуске Tilt
- ✅ Изменении файлов `infra/migrations/V*.sql`
- ✅ Рестарте `citus-coordinator`

### Схемы регистрируются при:
- ✅ Первом запуске Tilt
- ✅ Изменении файлов `infra/schemas/*.avsc`
- ✅ Рестарте `schema-registry`

### Микросервисы перезапускаются при:
- ✅ Изменении исходного кода
- ✅ Изменении Dockerfile
- ✅ Изменении k8s манифестов

---

## 🐛 Troubleshooting

### Миграции не применяются

**Проверить:**
```bash
# В Tilt UI найдите ресурс "apply-migrations"
# Посмотрите логи

# Или вручную:
cd infra
USE_K8S=1 K8S_NAMESPACE=dev-infra bash scripts/migrate.sh
```

### Схемы не регистрируются

**Проверить:**
```bash
# В Tilt UI найдите ресурс "register-schemas"
# Посмотрите логи

# Или вручную:
make register-schemas

# Проверить что зарегистрировано:
curl http://localhost:8081/subjects
```

### Tilt зависает на "Waiting..."

**Решение:**
```bash
# Проверить состояние подов
kubectl -n dev-infra get pods

# Посмотреть логи проблемного пода
kubectl -n dev-infra logs <pod-name>

# Если нужно - удалить и пересоздать
kubectl -n dev-infra delete pod <pod-name>
```

---

## 🎯 Best Practices

### ✅ Рекомендуется:
- Использовать `make tilt-up` для повседневной разработки
- Держать Tilt UI открытым для мониторинга
- Использовать live reload (автоматический)
- Коммитить миграции и схемы в git

### ❌ Не рекомендуется:
- Вручную применять миграции во время работы Tilt
- Редактировать файлы в `infra/` напрямую в кластере
- Пропускать миграции (нарушение порядка V1, V2, ...)
- Изменять уже применённые миграции

---

## 📊 Мониторинг инициализации

### Через Tilt UI (рекомендуется):
1. Откройте http://localhost:10350
2. Следите за статусом ресурсов:
   - 🟢 Green = Ready
   - 🟡 Yellow = Building/Starting
   - 🔴 Red = Error

### Через командную строку:
```bash
# Статус всех подов
watch kubectl -n dev-infra get pods

# Статус миграций
kubectl -n dev-infra logs -l app=apply-migrations --tail=50

# Статус регистрации схем
kubectl -n dev-infra logs -l app=register-schemas --tail=50
```

---

**Версия:** 1.0  
**Обновлено:** 2025-11-18  
**Совместимо с:** Tilt 0.33+, Kubernetes 1.28+
