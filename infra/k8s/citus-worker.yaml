apiVersion: v1
kind: Service
metadata:
  name: citus-worker
  namespace: dev-infra
  labels:
    app: citus-worker
spec:
  clusterIP: None
  selector:
    app: citus-worker
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: citus-worker
  namespace: dev-infra
spec:
  serviceName: citus-worker
  replicas: 1
  selector:
    matchLabels:
      app: citus-worker
  template:
    metadata:
      labels:
        app: citus-worker
    spec:
      containers:
        - name: postgres
          image: citusdata/citus:13.0 # upgraded for PG18 (ensure tag supports PG18)
          args: ["-c", "shared_preload_libraries=citus"]
          env:
            - name: POSTGRES_USER
              value: "app"
            - name: POSTGRES_PASSWORD
              value: "app"
            - name: POSTGRES_DB
              value: "app"
          ports:
            - containerPort: 5432
              name: pg
          readinessProbe:
            exec:
              command: ["pg_isready","-U","app","-d","app"]
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          emptyDir: {}

