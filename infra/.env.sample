# Core infrastructure
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=app
POSTGRES_PORT=5432

REDIS_PORT=6379

# Redpanda / Kafka
REDPANDA_EXTERNAL_KAFKA_PORT=19092
REDPANDA_ADMIN_PORT=9644

# Schema Registry
SCHEMA_REGISTRY_PORT=8081
SCHEMA_REGISTRY_URL=http://localhost:8081
# Compatibility policy (we set per-subject): BACKWARD_TRANSITIVE
SR_COMPATIBILITY=BACKWARD_TRANSITIVE

# Kubernetes
K8S_NAMESPACE=dev-infra
# To force Docker Compose instead of k8s for Make targets, run with:
# USE_DOCKER=1 make <target>

# Observability / Loki (for local clients)
# When running microservices outside the cluster, port-forward Loki first:
#   kubectl -n dev-infra port-forward svc/loki 3100:3100
# Endpoint to push logs (services must set tenant from request headers, not env):
LOKI_URL=http://localhost:3100

# Topics by context (TopicNameStrategy → subjects are {topic}-value)
TOPIC_BETS=V1_BETS
TOPIC_PAYMENTS=V1_PAYMENTS
TOPIC_BALANCES=V1_BALANCES
TOPIC_COMPLIANCE=V1_COMPLIANCE
TOPIC_SYSTEM=V1_SYSTEM
